<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC   
    "-//mybatis.org//DTD Mapper 3.0//EN"  
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.TrolleyDao">

	<select id="search" resultType="entity.Trolley">
		select * from trolley as t left
		join
		merchandise as m on t.merchandise_id=m.id where user_id=#{0}
	</select>

	<select id="searchByMid" resultType="entity.Trolley">
		select * from trolley where
		merchandise_id=#{0}
	</select>

	<insert id="add" parameterType="int">
		insert into
		trolley(count,user_id,merchandise_id)
		values(#{1},#{2},#{0})
	</insert>

	<insert id="addByDetail">
		insert into trolley(count,merchandise_id)
		values(#{0},#{1})
	</insert>

	<select id="searchAll" resultMap="TrolleyAndMerchandise">
		select
		m.id,m.name,m.detail,m.photo,m.price,t.id as tId,t.count from
		merchandise as m inner join trolley as t on m.id=t.merchandise_id
	</select>

	<resultMap type="entity.Trolley" id="TrolleyAndMerchandise">
		<id property="id" column="tId" />
		<collection property="merchandise" column="merchandise_id"
			ofType="entity.Merchandise">
			<id property="id" column="id" />
			<result property="detail" column="detail" />
			<result property="price" column="price" />
			<result property="count" column="count" />
			<result property="photo" column="photo" />
		</collection>
	</resultMap>

	<delete id="delete" parameterType="int">
		delete from trolley where
		id=#{0}
	</delete>

	<update id="update">
		update trolley set
		count=count+#{0} where
		merchandise_id=#{1}
	</update>


</mapper> 
    